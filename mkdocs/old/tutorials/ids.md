IDs have the format `type:idstring` for example `lights:loungeroom`.

These can be automatically mapped to human readable strings. See [strings](strings.md).

```js
var livingRoom = {
    lights: {
        set: function() {}
    }
};
```

